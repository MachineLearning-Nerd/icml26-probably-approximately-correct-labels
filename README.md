# Probably Approximately Correct Labels — independent reproduction

Clean-room local CPU/GPU reproduction for ICML 2026 paper `Yhv4d8wfbi`. See `STATUS.md`, immutable `contract/`, and retained source manifest. Claim 1 has a finite calibrated proxy-label toy with raw artifacts in `outputs/claim1_proxy_label_calibration/`; it is not a theorem verification. No HF cpu-upgrade, Jobs, paid compute, or remote GPU is used.
