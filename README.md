# Probably Approximately Correct Labels — independent reproduction

Clean-room local CPU/GPU reproduction for ICML 2026 paper `Yhv4d8wfbi`. See `STATUS.md`, the immutable `contract/` and retained source manifest. No HF cpu-upgrade, Jobs, paid compute, or remote GPU is used.
